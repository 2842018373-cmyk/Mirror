# Mirror 公网部署方案

## 方案对比

| 方案 | 费用 | 国内访问速度 | 自定义域名 | 适合场景 |
|------|------|------------|-----------|---------|
| **Cloudflare Pages** | 免费 | 中等（有CDN） | 支持 | 推荐首选 |
| **Vercel** | 免费 | 较慢 | 支持 | 海外用户为主 |
| **GitHub Pages** | 免费 | 较慢 | 支持 | 简单展示 |
| **阿里云 OSS** | 按量付费 | 快 | 支持 | 已有阿里云账号 |
| **腾讯云 COS** | 按量付费 | 快 | 支持 | 已有腾讯云账号 |

---

## 推荐方案：Cloudflare Pages（免费 + 全球CDN）

### 为什么选这个？
- 完全免费，无流量限制
- 全球 CDN 加速，国内访问比 GitHub Pages 快
- 支持自定义域名 + 免费 SSL 证书
- 部署简单，拖拽上传即可
- 支持自动部署（绑定 Git 仓库后）

### 部署步骤

#### 第一步：注册 Cloudflare 账号
1. 访问 https://dash.cloudflare.com/sign-up
2. 用邮箱注册，验证邮箱
3. 登录控制台

#### 第二步：创建 Pages 项目
1. 左侧菜单点击 **Pages**
2. 点击 **创建项目**
3. 选择 **直接上传**
4. 项目名称填写 `mirror-app`（或你喜欢的名字）
5. 点击 **创建项目**

#### 第三步：上传文件
1. 在项目页面点击 **上传资产**
2. 拖拽你的 `mirror-cover.html` 文件到上传区域
3. 等待上传完成

#### 第四步：设置入口文件
1. 上传完成后，点击 **自定义域** 或 **设置**
2. 确保默认页面设置为 `mirror-cover.html`
3. 保存设置

#### 第五步：访问网站
- Cloudflare 会给你一个默认域名：`https://mirror-app.pages.dev`
- 直接访问即可看到项目

#### 第六步：绑定自定义域名（可选）
1. 在 Pages 项目设置中点击 **自定义域**
2. 输入你的域名（如 `mirror.yourdomain.com`）
3. 按照提示在域名服务商处添加 CNAME 记录
4. Cloudflare 自动颁发免费 SSL 证书

---

## 备选方案：阿里云 OSS（国内访问快）

### 适用场景
- 主要用户在国内
- 已有阿里云账号
- 愿意支付少量费用（按流量计费，通常每月几毛钱）

### 部署步骤

#### 第一步：创建 OSS Bucket
1. 登录阿里云控制台 https://oss.console.aliyun.com
2. 点击 **创建 Bucket**
3. 填写 Bucket 名称（如 `mirror-app-2026`）
4. 地域选择 **华东1（杭州）** 或其他靠近你的地域
5. 存储类型选 **标准存储**
6. 读写权限选 **公共读**
7. 点击 **确定**

#### 第二步：开启静态网站托管
1. 进入 Bucket 详情页
2. 左侧菜单点击 **基础设置**
3. 找到 **静态页面** 选项，点击 **设置**
4. 开启静态网站托管
5. 默认首页填写 `mirror-cover.html`
6. 默认 404 页填写 `mirror-cover.html`
7. 保存

#### 第三步：上传文件
1. 进入 Bucket 的 **文件管理**
2. 点击 **上传文件**
3. 选择 `mirror-cover.html` 上传
4. 上传完成后，点击文件右侧的 **详情**
5. 复制 **URL**（如 `https://mirror-app-2026.oss-cn-hangzhou.aliyuncs.com/mirror-cover.html`）

#### 第四步：访问网站
- 直接访问 Bucket 的静态网站域名
- 格式：`http://mirror-app-2026.oss-cn-hangzhou.aliyuncs.com`

#### 第五步：绑定自定义域名（可选）
1. 在 Bucket 的 **传输管理** -> **域名管理** 中添加自定义域名
2. 在域名服务商处添加 CNAME 记录指向 OSS 域名
3. 在阿里云 **SSL 证书** 控制台申请免费证书
4. 绑定证书到自定义域名

---

## 域名购买建议

| 平台 | 价格 | 特点 |
|------|------|------|
| 阿里云万网 | .cn 约 30元/年 | 国内备案方便 |
| 腾讯云 DNSPod | .cn 约 30元/年 | 国内备案方便 |
| Namecheap | .com 约 80元/年 | 海外注册，无需备案 |
| Cloudflare Registrar | 成本价 | 无加价，但选择少 |

### 注意
- 如果使用国内服务器/阿里云 OSS + 自定义域名，需要 **ICP 备案**
- 如果使用 Cloudflare Pages + 海外域名，**无需备案**

---

## 快速部署检查清单

- [ ] 选择部署平台（推荐 Cloudflare Pages）
- [ ] 注册账号
- [ ] 上传 `mirror-cover.html`
- [ ] 设置默认首页
- [ ] 测试访问是否正常
- [ ] （可选）购买并绑定自定义域名
- [ ] （可选）配置 SSL/HTTPS

---

## 费用预估

| 方案 | 域名 | 托管 | 流量 | 总计/年 |
|------|------|------|------|--------|
| Cloudflare Pages + 免费子域名 | 0 | 0 | 0 | **0元** |
| Cloudflare Pages + 自定义域名 | ~30-80元 | 0 | 0 | **30-80元** |
| 阿里云 OSS + 自定义域名 | ~30元 | ~10元 | ~5元 | **~45元** |

---

## 推荐路径

**零成本方案**：Cloudflare Pages + 默认子域名（`your-project.pages.dev`）
- 完全免费
- 5分钟部署完成
- 适合比赛提交、快速分享

**低成本方案**：Cloudflare Pages + 购买一个 `.cn` 域名
- 年费约 30元
- 有自定义域名更专业
- 无需备案（域名在海外注册商）

**国内高速方案**：阿里云 OSS + 自定义域名
- 年费约 45元
- 国内访问速度快
- 需要 ICP 备案（约 7-20 天）
